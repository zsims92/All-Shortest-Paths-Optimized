These programs were run on the Stampede supercomputer.
Compiled using the icpc compiler
Run the make file to compile all code

Usage is as follows:
	./sequentialDS -n 1000 -s sum.txt
This will work for the sequential FW as well

For the openMP versions, make sure to run using the job scripts as they specify the number of processors.